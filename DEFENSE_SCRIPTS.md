# 答辩 Q&A 完整脚本

> 每道题给出 4 块: **老师可能这么问** (口语化, 不会逐字念稿子) / **你的完整回答** (1–2 分钟可背诵段落) / **白板要画什么** (ASCII 草图或简描) / **可能追问 + 应对** (老师常顺势挖深的问题)。
>
> 一共 15 道, 按提问频率排序, 答辩中**前 5 道几乎一定会问**。

---

## Q1 你这个项目要解决的是个什么问题?

**老师问**: "你能不能用一两分钟简单介绍下你这个项目?"

**你的回答**:
> 我们做的是**带专属卸货点的容量受限多行程路径规划问题**。背景是城市垃圾收运: 车辆从停车场 S 出发, 在 M×L 的网格化道路上访问若干带重量的收集点, 一旦载重达到上限就必须开到一个与 S 物理分离的处理厂 T 卸货, 卸货后可以继续作业, 直到所有收集点都被访问一次, 目标是**总行驶距离最短**。
>
> 这个问题相对经典 VRP 的关键不同点是: **起点 S 和卸货点 T 是分离的**, 所以"首次行程"(从 S 起) 和"后续行程"(从 T 起) 的距离矩阵在算法层必须区分对待, 不能简单地共用一套。
>
> 系统实现了五种算法 — 标准 DP、pivot 优化 DP、最近邻贪心、双车 DP、双车贪心, 用 C++17 写后端, PyQt6 写带动画的图形化前端, 通过纯文本协议解耦。

**白板**: 不画, 口述就行。如果一定要画, 画一个 3×3 网格示意 S/T/P 的位置。

**追问应对**:
- "为什么 S 和 T 要分离?" → 实际场景中停车场常在城郊, 处理厂在另一个郊区, 不会在同一处。
- "你的 n 最大测了多少?" → 任务书约束 n≤8, 我们测到 n=8, 实测 0.2 ms。

---

## Q2 你用了哪些算法? 各自的复杂度?

**老师问**: "讲讲你都用了哪些算法, 每个的时间空间复杂度。"

**你的回答**:
> 一共四种算法层级。
>
> 第一层是**最短路 BFS**, 因为网格是 4-邻接且边权恒为 1, 用 BFS 而不是 Dijkstra — Dijkstra 在边权常数时退化为 BFS 但多 $O(log(M L))$ 的堆操作。BFS 单次 $O(M L)$。
>
> 第二层是**距离矩阵预计算**: 对 $K = n + 2$ 个关键点每个跑一次 BFS, 同一次 BFS 的前驱表还用来 $O(L)$ 回溯所有目标点路径, 所以总复杂度是 $O(K cdot M L)$, 不是朴素的 $O(K^2 cdot M L)$。
>
> 第三层是**两层动态规划**: 内层是子集旅行商 DP, 状态 `tsp[mask][last]`, 复杂度 $O(2^n cdot n^2)$, 跑两次 (S-起一次, T-起一次); 外层是划分 DP, 状态 `G[mask]`, 转移枚举子集, 标准实现 $O(3^n)$, 我们做了基于 pivot 的对称性消除, 候选数从 $2^k - 1$ 减为 $2^(k-1)$。
>
> 第四层是**双车 DP**, 归约为 `singleCost[mask] + singleCost[full^mask]` 的二划分, 借助 `singleCost` 表的一次性求解 + 对称消除, 总复杂度还是 $O(3^n)$。
>
> 此外还有 $O(n^2)$ 的最近邻贪心作为对照基线。

**白板**:
```
BFS              O(M·L)            最短路
↓
距离矩阵          O(K·ML)           K=n+2 个关键点
↓
子集 TSP DP      O(2^n·n^2)        tsp[mask][i]
↓
划分 DP          O(3^n)            G[mask]
↓
singleCost      O(3^n)            派生表
↓
双车 DP          O(3^n)            归约 + 对称消除
```

**追问应对**:
- "$3^n$ 怎么来的?" → 子集枚举 $sum_("mask") 2^(|"mask"|) = 3^n$ (二项式定理)。
- "pivot 枚举减半为什么实测加速只 0-1%?" → 见 Q11。

---

## Q3 子集 TSP DP 的状态转移?

**老师问**: "把内层 DP 的状态和转移写出来。"

**你的回答**:
> 状态 `tsp[mask][last]` 表示"从 depot 出发, 访问了 mask 中的所有点, 当前停在 last 这个收集点"的**最小代价**。mask 是位掩码, last ∈ [0, n)。
>
> 初始化: 对每个单点 i, `tsp[1<<i][i] = D[depot][P_i]`, 其它全 INF。
>
> 转移: 枚举 mask 中所有 last (即 `mask & (1<<last)` 非零的 last), 再枚举 nxt 不在 mask 中, 尝试扩展:
> `tsp[mask | (1<<nxt)][nxt] = min(自身, tsp[mask][last] + D[P_last][P_nxt])`
>
> 由 tsp 派生 `firstCost[mask] = min_last tsp[mask][last] + D[last][T]`, 表示"S 起、走完 mask、回到 T"的最优代价。`laterCost` 类似但 depot 换成 T。容量超 $W_("max")$ 的 mask 直接置 INF。

**白板**:
```
tsp[mask][last] = min cost to start at depot,
                  visit all of mask, end at P_last

Init:      tsp[1<<i][i] = D[depot][P_i]
Transit:   tsp[mask|1<<nxt][nxt]
           = tsp[mask][last] + D[P_last][P_nxt]
           ∀ last ∈ mask, ∀ nxt ∉ mask

Derive:    firstCost[mask] = min_last tsp[mask][last] + D[P_last][T]
           laterCost[mask] = (同样, depot 换 T)
           filter: sum_i w_i > W_max → INF
```

**追问应对**:
- "为什么要算两次 tsp (S 起和 T 起)?" → 首程从 S 出发, 后续行程从 T 出发, 距离矩阵不同, 必须分开。
- "状态空间 $2^n cdot n$ 在 n=8 时多大?" → 256 × 8 = 2048 个状态。

---

## Q4 外层划分 DP 怎么把多行程切开?

**老师问**: "你说划分 DP, 它怎么切?"

**你的回答**:
> 状态 `G[mask]` 表示"**仅用 later-trip** (即从 T 出发的行程) 收完 mask 中所有点的最小总代价"。$G(emptyset) = 0$。
>
> 转移: 把 mask 切一刀, 第一刀是一个非空子集 Q, 这部分作为一条 later-trip, 剩下递归:
> $G["mask"] = min_(Q subset.eq "mask", Q != emptyset) "laterCost"(Q) + G["mask" without Q]$
>
> 顶层求解时, 首程 (S-起) 单独拆出来:
> $"OPT" = min_(Q_1 subset.eq [n], Q_1 != emptyset) "firstCost"(Q_1) + G([n] without Q_1)$
>
> 这种"先 first 后 later"的分离是因为 S 和 T 物理分离, 第一次行程的起点不同。报告里的引理 1 用归纳法证了 G 表的最优性。

**白板**:
```
G[mask] = "仅用 later-trip 收完 mask 的最优总代价"

G[∅] = 0
G[mask] = min_{Q⊆mask, Q≠∅}
          laterCost(Q) + G[mask\Q]

最终答案:
OPT = min_{Q1⊆[n], Q1≠∅}
      firstCost(Q1) + G([n]\Q1)
```

**追问应对**:
- "怎么知道这个 DP 是对的?" → 报告引理 1 的归纳证明: 假设对所有 mask' ⊊ mask 都成立, 取 mask 的最优分解, 它的首条 later-trip 必然是某个 Q, 剩余按归纳是 G[mask\Q]; 因此 G[mask] ≤ 全局最优 ≤ G[mask], 取等。
- "Q 的枚举方式有几种?" → 见 Q5。

---

## Q5 pivot 子集枚举为什么等价?

**老师问**: "你说用分治法把候选数从 $2^k-1$ 减到 $2^(k-1)$, 这怎么证等价?"

**你的回答**:
> 关键观察: 对 mask 选定一个固定元素 pivot, 所有非空子集 Q 按"pivot 是否属于 Q"一分为二:
> - 分支 A: $"pivot" in Q$, 显式枚举, 候选数 $2^(|"mask"|-1)$;
> - 分支 B: $"pivot" in.not Q$, 此时 $"mask" without Q$ 含 pivot, $G("mask" without Q)$ 在递归求解时必然枚举到含 pivot 的子集, 故该分支被递归子问题覆盖。
>
> 但这个"覆盖"等价的前提是: **同一 mask 内 later-trip 的执行顺序不影响总代价**。这是报告的引理 2 "后续行程顺序可交换性", 因为每条 later-trip 都从 T 起、回 T 终, 行程之间没有共享状态, 顺序无关。
>
> 借助引理 2, 引理 1, 报告的定理 1 完成了 pivot 枚举与标准枚举等价的严格证明。**最优值完全相同, 只是常数减半**。

**白板**:
```
mask 的所有非空子集 Q:
    分支 A (pivot ∈ Q): 显式枚举, 2^(k-1) 个
    分支 B (pivot ∉ Q): 由 G[mask\Q] 递归覆盖 (mask\Q 含 pivot)

引理 2 (顺序可交换)
+ 引理 1 (G 表最优)
→ 定理 1 (pivot 枚举与标准枚举等价)
```

**追问应对**:
- "如果 pivot 选错了会不会有问题?" → 不会, 任何 pivot 都行, 我们选 `mask & -mask` (最低位) 因为最快算。
- "实测加速是多少?" → 见 Q11。

---

## Q6 双车协同的核心思路 (创新点)

**老师问**: "双车协同你是怎么做的? 比单车 DP 重复跑两次还是?"

**你的回答**:
> 这是项目里我最得意的优化。核心思路是**把双车答案归约为单车 singleCost 表的二划分**:
>
> $"OPT"_("双车") = min_(A subset.eq [n]) "singleCost"[A] + "singleCost"[[n] without A]$
>
> 其中 `singleCost[mask]` 是预先一次性算出来的"单车从 S 出发收完 mask 的最优代价", 对全集 $[n]$ 的所有 $2^n$ 个子集都已经填表。
>
> 关键加速来自两点:
> 1. **不重建子距离矩阵**: 朴素实现对每个 A 都要构造一个只包含 A 中点的子实例, 重新 BFS、重新做距离矩阵、重新跑子 DP, 复杂度爆炸。我们让单车 DP 在主入口就一次性算出 `singleCost[mask]` for all mask ⊆ [n], 双车求解阶段只是查表 + 加法。
> 2. **对称性消除**: $(A, B)$ 和 $(B, A)$ 是同一个无序划分, 强制 $0 in A$ 后, $2^n$ 的二重枚举减半为 $2^(n-1)$。$A = emptyset$ 单独处理 (退化为单车场景)。
>
> **实测加速 460×**: P6 优化前大样本双车求解 ~92 ms, 优化后 ~0.2 ms, 与单车 DP 运行时间持平。

**白板**:
```
双车归约:
    OPT_dual = min_{A⊆[n]} singleCost[A] + singleCost[[n]\A]

singleCost[mask] 是 dp.cpp 一次性算出的, 复用 firstCost / G 表:
    singleCost[mask] = min_{Q1⊆mask, Q1≠∅}
                       firstCost(Q1) + G[mask\Q1]

对称消除: 强制 0 ∈ A, 二重枚举减半

实测: 460× 加速, 双车 DP ≈ 单车 DP
```

**追问应对**:
- "为什么是 460× 这么夸张?" → 朴素实现每个 A 都重新 BFS + 子 DP, 当 A 大时几乎重复一次完整 DP。优化后只查表, 0 BFS, 0 子 DP。
- "你怎么知道是 460×?" → 报告 §6.7.2 表格, 大样本上从 92 ms 降到 0.2 ms。

---

## Q7 这个项目你做了什么创新?

**老师问**: "你觉得相对其他同学, 你的项目有什么不一样的地方?"

**你的回答**:
> 三个方面。
>
> 第一是**算法层有真优化**。双车协同我们用 `singleCost[mask]` 一次性中间表 + 对称性消除, 把双车 DP 从朴素的 $O(2^n cdot "单车 DP")$ 压到 $O(3^n)$, 实测 460 倍加速。多数同学的双车实现就是简单枚举划分跑两次单车 DP。
>
> 第二是**有完整的形式化证明**。报告里有 12 个定义/引理/定理块, 用归纳法严格证明了划分 DP 的最优性 (引理 1) 和 pivot 枚举对标准枚举的等价性 (定理 1), 后者还引入了一个辅助引理 (引理 2 顺序可交换性) 来支撑。这不是白话描述。
>
> 第三是**实验数据扎实且诚实**。我们写了独立的暴力对拍 — 第二份 BFS + 全排列 + 切分点全枚举, 跟 solver 的 DP 完全不共享代码, 跑了 720 例 0 mismatch, 是 DP 正确性的直接证据。还做了 1940 个数据点的密集基准, 跨 6 个 n 值 × 4 个障碍密度。结果**很反直觉**: pivot 枚举理论 2× 但实际只 0-1%, 我们没夸大, 在报告里如实写了"因为子集枚举只占总开销的一小部分"。

**白板**: 不用画, 用嘴讲。

**追问应对**:
- "为什么 pivot 没快多少你还实现?" → 因为它是任务书的加分项 (3), 必须实现; 而且它的"理论 - 实测 gap"本身是一个有教学价值的发现。
- "暴力对拍能确保 100% 对吗?" → 在 n ≤ 5 的实例上是的, 因为 n!·2^(n-1) 全枚举就是定义上的最优。所有 720 例都 ≤ n=5, 完全可信。

---

## Q8 为什么用 BFS 不用 Dijkstra 或 A*?

**老师问**: "网格最短路你用了 BFS。Dijkstra 也能做, 为什么不选?"

**你的回答**:
> 三个原因。
>
> 一是**边权问题**。BFS 假设所有边权相同 (本问题恒为 1), 这正是 4-邻接网格的情况。Dijkstra 是对边权非负的更一般算法, 当边权为常数时会退化为 BFS 的行为, 但多了一个堆操作的 $O(log(M L))$ 因子, 没有任何收益。
>
> 二是**实现复杂度**。BFS 用普通队列, 30 行代码就能写好。Dijkstra 要优先队列, 还要处理松弛更新, 代码量翻倍, 没必要。
>
> 三是**A\* 对这个问题没用**。A\* 需要一个可采纳的启发式 (比如曼哈顿距离), 但我们要做的是**全对最短路** (距离矩阵的 $K times K$), 不是单源单目标。A\* 在多源多目标场景下没有比 BFS 更快。

**白板**:
```
                单源单目标   全对最短路   常数边权
BFS              ✓            ✓ (跑 K 次)   ✓
Dijkstra        ✓ + log K    ✓ + log K    退化为 BFS
A*              ✓ (启发式)   ✗ 无优势      —
```

**追问应对**:
- "如果网格变成有不同通行成本 (比如山地)?" → 那就上 Dijkstra, BFS 不再适用。
- "A* 的启发式什么时候有用?" → 单源单目标 + 大图 + 启发式紧 (像 Manhattan), 比如游戏里的寻路。

---

## Q9 输入不合法怎么处理?

**老师问**: "如果用户输入了不合法的数据, 你的程序怎么反应?"

**你的回答**:
> 我们把这部分单独抽出来做了一个 `feasibility` 模块, 在算法分派之前一次性检查 6 类约束:
>
> 1. 所有关键点 (S, T, P_i) 都得落在 `.` 格, 不能越界或在障碍上
> 2. S ≠ T (起点不能等于终点)
> 3. 任何两个关键点坐标不能重复
> 4. 所有 $w_i in [1, 3]$
> 5. $W_("max") >= max_i w_i$ 且 $sum_i w_i > W_("max")$
> 6. 所有关键点都与 S 连通 (用一次 BFS 检查)
>
> 任何一项失败立即返回 `STATUS infeasible / REASON <详细文本>`, GUI 前端解析后弹出错误对话框, 用户可以立即在网格上修改并重新运行。报告 §3.3 有完整的 REASON 文本表, `tests/verify_sample_library.py` 在样例库的 8 个不可行用例上自动校验 REASON 分类。
>
> 整个 check 开销不超过一次 BFS, 不是性能瓶颈。

**白板**:
```
用户输入 → check_feasibility (6 类静态检查 + 1 次 BFS)
              ↓ 通过
            算法分派 (dp/greedy/dual...)
              ↓
            STATUS ok ... + 路径
              ↓ 不通过
            STATUS infeasible / REASON <text>
              ↓
            GUI 弹错误框 → 用户重新编辑
```

**追问应对**:
- "如果用户故意构造一个 S 和 T 都被障碍包围的情况?" → BFS 那一步会发现"P_i 与 S 不可达", 返回 REASON。
- "REASON 文本是约定的吗?" → 是的, 写在样例库 README, 测试脚本以此为契约。

---

## Q10 PyQt 是怎么和 C++ 通信的?

**老师问**: "前端 Python 怎么调到后端 C++?"

**你的回答**:
> 通过**子进程 + 文本协议**, 不是绑定也不是嵌入。
>
> 具体流程: 前端 `controller.py` 用 `subprocess.run` 调用 `solver.exe`, 把当前编辑的实例写到临时文件传给它, 然后从 stdout 读求解结果。默认是行式协议 (每行一个字段, 比如 `STATUS ok`、`TOTAL_DISTANCE 34`、`TRIP 1 LOAD 3 DIST 14`、`PATH 0,0 0,1 ...`), 也支持 `--json` 切换成结构化 JSON 给外部工具用。
>
> `parse_solver_output` 是一个状态机解析器, 逐行读 STATUS/ALGORITHM/VEHICLE/TRIP/PATH 等字段, 构造 Python 端的 `Solution` 对象。
>
> 这种解耦的好处: ① C++ 不依赖 Python (反过来也一样), 两边可独立开发测试; ② solver.exe 可以被任何其他上层应用调用 (比如 Web 后端、命令行批量测试脚本); ③ 测试简单, 直接 `solver.exe input.txt` 一行命令就能验证。

**白板**:
```
GUI (PyQt)                           solver.exe (C++)
─────────                            ─────────────────
controller.py                         main.cpp
   ↓                                     ↑
subprocess.run                          stdin
   →  writes input.txt                    ↓
   →  calls solver.exe input.txt          parses → solve → emits stdout
   ←  reads stdout                        ↓
   ←  parse_solver_output                stdout (行式 or JSON)

解耦: 0 链接依赖, 任意上层应用都可调
```

**追问应对**:
- "为什么不用 pybind11 直接绑定?" → 那样 C++ 必须做成共享库, 接口管理麻烦, 不如纯进程隔离干净。
- "subprocess 性能开销大不大?" → 单次进程启动 ~50 ms, 我们的算法本身 < 1 ms, 启动开销主要影响 GUI 体验, 不影响算法评估。

---

## Q11 为什么分治没快多少?

**老师问**: "你说分治枚举把候选数减半, 那时间应该减半, 但你报告里说只快了 0-1%, 怎么回事?"

**你的回答**:
> 这是我们密集基准实验的一个反直觉发现。理论上 pivot 枚举把 G[mask] 的候选数从 $2^k - 1$ 减为 $2^(k-1)$, 总枚举量减半。但实际跑 1940 个数据点 (跨 n=3..8 × 4 种障碍密度), `dp_dc` 和 `dp` 的端到端时间比稳定在 0.99 到 1.04 之间, 几乎不可观察。
>
> 原因是: **子集枚举只占完整 DP 流程开销的一小部分**。一次完整求解包括 ① 距离矩阵预计算 (K 次 BFS); ② 子集 TSP DP 填表 ($O(2^n n^2)$); ③ 容量过滤; ④ G 表填充 (这一步才是子集枚举); ⑤ singleCost 表; ⑥ 路径回溯。其中 ④ 只是其中一步, 其余几步占的常数加起来更大。子集枚举本身减半, 平摊到端到端就稀释了。
>
> 这是一个**诚实的实验结果**, 我们没有为了凑加分项数字而夸大。报告里如实写了"理论 2× 但实测 0-1%", 并解释了原因。

**白板**:
```
一次完整 DP 求解的开销分布 (示意):
┌─────────────────────────────────────────┐
│ ① 距离矩阵 BFS        ~30%              │
│ ② 子集 TSP DP          ~25%              │
│ ③ 容量过滤              ~5%              │
│ ④ G 表 (子集枚举)      ~20%  ← pivot 优化在这里
│ ⑤ singleCost           ~15%              │
│ ⑥ 路径回溯               ~5%              │
└─────────────────────────────────────────┘

pivot 优化使 ④ 减半: 0.20 → 0.10
端到端从 1.00 → 0.90, 即 10% 提升
但实测 0-1%, 因为其它步骤的实测占比更大
```

**追问应对**:
- "那 pivot 优化白做了?" → 不是, 在更大 n 上 ④ 的占比会显著上升, 优化会显现; 而且任务书要求加分项 (3) 必须实现; 而且这个"理论 - 实测 gap"本身是值得报告的发现。
- "怎么验证你的实验真的跑了 1940 个点?" → 数据全部保存在 `tests/dense_benchmark_log.csv`, 4 张图在 `docs/figures/bench_*.png`。

---

## Q12 贪心什么时候会失败?

**老师问**: "你的贪心算法可靠吗? 什么时候会出错?"

**你的回答**:
> 实现层面**不会失败** — 我们在最近邻贪心里硬过滤了载重约束和 BFS 可达性, 所以在通过可行性检查的输入上, 贪心一定能完成访问全部点。在 372 例随机样本上, 贪心成功率 100%, 无超载、无漏点。
>
> 但**最优性上会失败**。因为最近邻是局部决策, 缺乏全局视野, 容易陷入"近邻陷阱"。报告 §6.5 构造了一个 $3 times 10$ 的对抗实例, 把 P_0 故意放在 S 旁边, 让贪心首步抓 P_0, 但最优策略是先抓 P_1 (略远但能凑成饱载首程)。结果 DP 最优 13, 贪心 15, 差 15.4%。
>
> 在 338 例随机可行样本上, 贪心严格次优的占 31% (105 例), 平均超出 DP 解 14.5%, 最坏一例 (seed=131, n=5) DP=30 但贪心=66, 超出 120%。
>
> 所以**贪心可以作为快速基线 (O(n^2) 几乎瞬时), 但不能当成最优解**, 在结构性输入上代价不可忽略。

**白板**:
```
对抗实例 (3×10 网格):
   S P0 . . . . . . P2 T
   .  .  . . . . . . .  .
   . P1  . . . . . . .  .

w=(1,1,1), Wmax=2
DP 最优: S→P1→P0→T→P2→T = 13 (P1 先抓凑饱载)
贪心:     S→P0→T→P1→T→P2→T = 15 (P0 最近, 但单点首程浪费)

→ 贪心 15.4% 次优, 因为局部决策缺乏全局视野
```

**追问应对**:
- "怎么改进贪心?" → 2-opt 或 Or-opt 局部搜索可以 $O(n^2)$ 额外代价补一部分, 但不会到最优。
- "贪心成功率怎么测的?" → `/tmp/greedy_rate.py` 脚本跑 372 例 (62 种子 × 3 n × 2 rho)。

---

## Q13 你怎么证明 DP 实现是对的?

**老师问**: "你的 DP 代码也可能有 bug, 你怎么知道它对?"

**你的回答**:
> 三层证据。
>
> **第一层是数学证明**。报告 §5 用归纳法严格证明了 G 表的最优性 (引理 1) 和 pivot 枚举的等价性 (定理 1)。这保证了**算法在数学上是对的**。
>
> **第二层是独立暴力对拍**。我们写了 `tests/brute_force_checker.py`, 用**独立的第二份 BFS** 算距离矩阵 (不调 solver 内部代码), 再 `itertools.permutations` 全排列收集点访问顺序 ($n!$ 种), 对每个排列用二进制位枚举切分点 ($2^(n-1)$ 种), 校验载重约束并计算总代价, 取全局最小。这是**定义上的最优**, 完全没有 DP 的影子。
>
> 跑了 720 例 (60 种子 × 3 个 n 值 × 2 个障碍密度 × `dp` 与 `dp_dc` 两种算法), 结果 **658 PASS / 0 MISMATCH / 62 SKIP** (SKIP 是不可行实例被暴力和 solver 同步识别)。0 失配是**实现正确性的直接证据**。
>
> **第三层是密集基准**。1940 个数据点上, `dp` 和 `dp_dc` 输出值在所有 n 上严格相同, 与定理 1 的等价性论断一致。

**白板**:
```
正确性验证三层:
┌────────────────────────┐
│ 1. 数学证明 (引理1, 定理1) │  → 算法层
├────────────────────────┤
│ 2. 暴力对拍 720 例       │  → 实现层
│    n!·2^(n-1) 全枚举      │
│    独立第二份 BFS          │
│    0 MISMATCH            │
├────────────────────────┤
│ 3. 密集基准 1940 数据点  │  → 等价性
│    dp ≡ dp_dc            │
└────────────────────────┘
```

**追问应对**:
- "暴力对拍能扩展到 n=8 吗?" → 不能, n=8 时 $40320 cdot 128 = 5.2 times 10^6$, 单点要分钟级。暴力只能对小规模 (n≤5)。
- "为什么不直接拿暴力当算法用?" → 复杂度 $n! cdot 2^(n-1)$, n=8 就已经接近分钟级, n=10 不可行。暴力只用来对拍。

---

## Q14 如果 n 变得很大怎么办?

**老师问**: "你的 $n <= 8$ 是任务书约束。如果实际场景 n=20 你的算法还行吗?"

**你的回答**:
> 不行, 必须换路。我们的精确 DP 复杂度是 $O(3^n)$, n=8 时 $6561$ 状态秒级求解, 但 n=12 是 $531441$, n=20 是 $3.5 times 10^9$, 完全不可行。
>
> 几个可能的延伸方向:
>
> 1. **Meet-in-the-Middle**: 把状态空间从 $Theta(2^n)$ 拆成两半合并, 状态规模降到 $Theta(2^(n/2))$, 有望让 n=16 仍能精确求解;
> 2. **列生成 (Column Generation)**: 把外层划分 DP 重新建模为整数规划, 用 Dantzig-Wolfe 分解, 适用于任意 V 辆车的扩展;
> 3. **启发式 + 局部搜索**: 最近邻贪心 + 2-opt / Or-opt 局部搜索, 牺牲最优性换可行的多项式时间;
> 4. **元启发式**: 模拟退火、遗传算法、Lin-Kernighan, 在 n=30..100 范围常用。
>
> 我们的报告 §7 把这些列为未来工作。任务书约束 n=8, 所以本项目专注于精确求解 + 严格证明, 这是结构化的取舍。

**白板**:
```
n            状态规模 (3^n)    可行性
3            27               瞬时
5            243              瞬时
8            6561             ms 级 (我们的工作)
10           59049            秒级
12           531441           分钟级 (Meet-in-Middle 救援)
16           4.3×10^7         需要 Meet-in-Middle
20           3.5×10^9         完全无法精确求解
                              → 必须用启发式 / 列生成 / 元启发式
```

**追问应对**:
- "Meet-in-the-Middle 怎么做?" → 把收集点分两半 $A cup B = [n]$, 分别预算 DP, 然后枚举合并点。我们没实现, 是未来工作。
- "列生成具体怎么用?" → 主问题是路径分配的整数规划, 子问题是单车最优行程枚举, 不展开。

---

## Q15 GUI 那些"实时载重 / 已行驶距离"是怎么刷新的?

**老师问**: "动画的时候右边的载重和已行驶距离一直在跳, 怎么实现的?"

**你的回答**:
> 通过 **`Animator` 暴露的 `status_callback` 钩子**。
>
> Animator 给每辆车配一个独立的 `QVariantAnimation`, 单步时长 180 ms, `InOutQuad` 缓动曲线, 起点和终点是相邻两格的场景坐标。`valueChanged` 信号每帧驱动 sprite 平滑移动, `finished` 触发下一格的动画启动 (链式推进)。
>
> 在四个时机, Animator 会调用 `status_callback(vi, info)` — 构造完成 (初始态)、每完成一格、trip 切换、全部完成。info 字段包括 `trip_idx / num_trips / load / distance / done`。
>
> GUI 主窗口的 `_on_animation_status` 接收回调, 更新对应车辆行的文字 (比如 "车 1 — Trip 2/3, 载重 3, 已行驶 12"), 完成时整行背景从浅蓝切换为浅绿, 配合右上状态徽章 (等待→运行中→完成→错误) 的颜色变化。
>
> 整个机制是事件驱动的, 不轮询、不阻塞主线程, UI 响应流畅。

**白板**:
```
Animator (一辆车一个)
  ┌─────────────────────┐
  │ QVariantAnimation   │  180 ms, InOutQuad
  │ start → end (sprite移动)
  └─────────────────────┘
      ↓ finished
  _on_step_done(vi)
      ↓
  distance_done[vi] += 1
      ↓
  status_callback(vi, info)  → 主窗口
      ↓
  _on_animation_status
      ↓
  更新文字 + 切背景色 + 更新徽章
```

**追问应对**:
- "为什么不是定时轮询?" → 轮询消耗 CPU 且与动画帧不对齐, 事件驱动更优雅。
- "多车情况下回调会冲突吗?" → 不会, 回调串行执行 (Qt 主线程事件循环), info 中的 vi 字段标识哪辆车。

---

## 最后: 答辩心态

1. **数字记牢**: 720 例 / 1940 数据点 / 460× 加速 / 14.5% 平均次优 / 36 页报告 — 老师抓数字
2. **白板尽量画**: 画图比口述显得更有把握, 哪怕画得简单
3. **诚实**: 不知道就说"这点我之前没想过, 但我的猜测是 ..."
4. **创新点引导**: Q7 这种开放性问题主动讲 singleCost + 460× + 暴力对拍, 把节奏拿在自己手里
5. **承认局限**: 比如 pivot 加速只 0-1%, 比如 n=8 是上限, 这些都不是缺点而是诚实
