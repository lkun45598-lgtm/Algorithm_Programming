# 算法手把手推演

> 这份文档用一个**最小但完整**的样例 (3×3 网格, n=2), 把项目里**每个算法**的状态转移、表格填充、最终答案的来源, 一格一格地走一遍。所有数字都是手算可验证的, 不是抽象描述。

---

## 0 教学样例

我们选最小的可行实例: **3×3 网格, 2 个收集点, 强制多行程**。

```
列:  0 1 2
行0: S . P0
行1: . . .
行2: P1 . T
```

形式化定义:
- $M = L = 3$, 网格全部可通行 (无障碍)
- $S = (0, 0)$, $T = (2, 2)$
- $P_0 = (0, 2)$, $P_1 = (2, 0)$
- $w_0 = 2$, $w_1 = 2$
- $W_("max") = 3$

验证可行性约束 ($max_i w_i <= W_("max") < sum_i w_i$): $max = 2 <= 3 < 4 = sum$ ✓

**直觉解**: 单车两次往返, $S -> P_0 -> T$ (载重 2, 距离 4), $T -> P_1 -> T$ (载重 2, 距离 4), 总距离 = 8。我们将通过 DP 严格推出这个答案。

---

## 1 BFS 与距离矩阵

后端先把 $K = n + 2 = 4$ 个关键点统一编号:
- `IDX_S = 0`, `IDX_T = 1`, `IDX_P0 = 2`, `IDX_P1 = 3`

然后对每个源点跑一次 `bfsWithPrev`, 得到从该源点到所有点的最短距离 + 前驱表。无障碍 3×3 网格上距离就是曼哈顿距离:

**距离矩阵 $D[4][4]$**:

| | S | T | P0 | P1 |
|---|---|---|---|---|
| **S** | 0 | 4 | 2 | 2 |
| **T** | 4 | 0 | 2 | 2 |
| **P0** | 2 | 2 | 0 | 4 |
| **P1** | 2 | 2 | 4 | 0 |

注意 $D[P_0][P_1] = 4$ 而不是 2: 路径必须沿 $P_0 -> (0,1) -> (0,0) -> (1,0) -> P_1$ 或对称, 总 4 步。

**复杂度**: 4 次 BFS, 每次 $O(M cdot L) = O(9)$, 总 $O(K cdot M L)$。路径回溯 (prev 表) 单次 $O(L)$ 其中 $L$ 是路径长度。

---

## 2 子集 TSP DP (内层)

**状态**: `tsp[mask][i]` = "从 depot 出发, 访问 mask 中全部收集点, 以 $P_i$ 结尾的最小代价"。

mask 取值 0-3 (即 00, 01, 10, 11), 对应"包含 $P_0$ 否 / 包含 $P_1$ 否"。

**从 $S$ 出发**:

| mask | last=0 (P0) | last=1 (P1) | 说明 |
|---|---|---|---|
| 00 | — | — | 空集, 不进入循环 |
| 01 ({P0}) | **2** | INF | 起步: $D[S][P_0] = 2$ |
| 10 ({P1}) | INF | **2** | 起步: $D[S][P_1] = 2$ |
| 11 ({P0,P1}) | **6** | **6** | 见下 |

mask=11 的两个值怎么来的:
- `tsp[11][0]` = `tsp[10][1]` + $D[P_1][P_0]$ = 2 + 4 = **6** (先去 $P_1$, 再到 $P_0$)
- `tsp[11][1]` = `tsp[01][0]` + $D[P_0][P_1]$ = 2 + 4 = **6** (先去 $P_0$, 再到 $P_1$)

**从 $T$ 出发** (相同结构, 因为本例对称):

| mask | last=0 | last=1 |
|---|---|---|
| 01 | 2 | INF |
| 10 | INF | 2 |
| 11 | 6 | 6 |

---

## 3 firstCost / laterCost

**派生定义**: 在 tsp 表的基础上, 加上"末尾点到 T 的距离", 取 last 维度上的最小:

$"firstCost"["mask"] = min_("last" in "mask") "tsp"_S["mask"]["last"] + D["last"][T]$

$"laterCost"["mask"] = min_("last" in "mask") "tsp"_T["mask"]["last"] + D["last"][T]$

**filtering**: 容量超限 ($sum_(i in "mask") w_i > W_("max")$) 的 mask 直接置 INF。

| mask | $sum w_i$ | firstCost | laterCost |
|---|---|---|---|
| 00 | 0 | (空 trip 无意义) | (空 trip 无意义) |
| 01 | 2 | $2 + 2 = 4$ | $2 + 2 = 4$ |
| 10 | 2 | $2 + 2 = 4$ | $2 + 2 = 4$ |
| 11 | 4 > 3 | **INF** | **INF** |

mask=11 被容量过滤掉了 — 这意味着一次行程不能同时拿走两个点。

---

## 4 划分 DP $G["mask"]$

**状态**: $G["mask"]$ = "**仅用 later-trip** (即从 T 出发的行程) 收完 mask 中所有点的最优总代价"。

**转移**: 把 mask 切一刀, 第一刀是一个非空子集 Q, 剩下递归:

$G["mask"] = min_(Q subset.eq "mask", Q != emptyset) "laterCost"(Q) + G["mask" without Q]$

边界: $G(emptyset) = 0$。

**填表过程** (按 mask 升序):

**`G[00] = 0`** (空集, 不需要 trip)

**`G[01]`** (mask = {P0}):
- 唯一非空子集 Q = {P0}:
  - $"laterCost"[01] + G[00] = 4 + 0 = 4$
- $G[01] = bold(4)$, pickG[01] = 01

**`G[10]`** (mask = {P1}):
- 唯一非空子集 Q = {P1}:
  - $"laterCost"[10] + G[00] = 4 + 0 = 4$
- $G[10] = bold(4)$, pickG[10] = 10

**`G[11]`** (mask = {P0, P1}):
- Q = {P0,P1}: $"laterCost"[11] + G[00] = "INF" + 0 = "INF"$ ❌
- Q = {P0}: $"laterCost"[01] + G[10] = 4 + 4 = 8$
- Q = {P1}: $"laterCost"[10] + G[01] = 4 + 4 = 8$
- $G[11] = bold(8)$, pickG[11] = 01 (取第一个最小)

**完整 G 表**:

| mask | G[mask] | pickG (第一条 later-trip) |
|---|---|---|
| 00 | 0 | — |
| 01 | 4 | {P0} |
| 10 | 4 | {P1} |
| 11 | 8 | {P0} |

---

## 5 pivot 枚举对比 (加分项 3)

`G[11]` 的填充过程, 我们刚才**遍历了 3 个 Q** (Q=11, Q=10, Q=01)。pivot 枚举只遍历**包含最低位元素的 Q**, 即只看 Q ∋ {P0}。

**pivot 枚举的过程**:
- pivot = 11 & -11 = 01 (最低位是 P0)
- rest = 11 ^ 01 = 10 (P1)
- 遍历 R ⊆ rest = {10, 00}, Q = pivot | R:
  - R = 10 → Q = 01 | 10 = **11** (含 pivot, 试)
  - R = 00 → Q = 01 | 00 = **01** (含 pivot, 试)
- 只遍历了 **2 个** Q, 跳过了 Q = 10 = {P1} (不含 pivot)

**对照表**:

| 枚举方式 | 遍历的 Q | 候选数 |
|---|---|---|
| 标准枚举 | {00, 01, 10, 11} 中所有 ≠ 0 的: {01, 10, 11} | **3** |
| pivot 枚举 | 仅含 P0 的: {01, 11} | **2** |
| 一般公式 | $2^k - 1$ vs $2^(k-1)$ | (k = $|"mask"|$) |

**为什么等价?** 跳过的 Q = {P1} 不含 pivot, 但它对应的"leftover" mask\Q = {P0} 含 pivot, 这个 leftover 会作为 $G[01]$ 被递归求解, 而 $G[01]$ 又会枚举含 pivot 的子集 (Q'={P0}) — 等价于"切一刀, 拿走 {P0} 这个 trip, 剩下的递归处理"。

报告 §5 的**定理 1** 严格证明了这个等价性, 关键依赖**引理 2 (后续行程顺序可交换性)**: 同一 mask 内 later-trip 的执行顺序不影响总代价, 所以"先 trip {P0} 再 trip {P1}"和"先 trip {P1} 再 trip {P0}"代价相同, pivot 枚举只保留前者的顺序无害。

---

## 6 `singleCost[mask]` 派生 (创新点 ⭐)

**状态**: `singleCost[mask]` = "**单车从 S 出发**收完 mask 全部点的最优总代价 (含首程 + 后续行程)"。

**转移**: 把首程 (S-起) 单独拆出来:

$"singleCost"["mask"] = min_(Q_1 subset.eq "mask", Q_1 != emptyset) "firstCost"(Q_1) + G["mask" without Q_1]$

**为什么需要这张表?** 双车 DP 的归约是 $min_(A) "singleCost"[A] + "singleCost"[[n] without A]$。**如果对每个 A 重新建距离矩阵 + 重跑子 DP**, 复杂度爆炸; **如果先一次性算出全集所有子集的 singleCost**, 双车只是查表加法, 复杂度大降。

**填表过程**:

**`singleCost[00] = 0`** (空集, 单车不出动)

**`singleCost[01]`** (mask = {P0}):
- 唯一 $Q_1$ = {P0}: $"firstCost"[01] + G[00] = 4 + 0 = 4$
- $"singleCost"[01] = bold(4)$, bestQ1 = {P0}

**`singleCost[10]`** (mask = {P1}):
- $Q_1$ = {P1}: $4 + 0 = 4$
- $"singleCost"[10] = bold(4)$, bestQ1 = {P1}

**`singleCost[11]`** (mask = {P0, P1}):
- $Q_1$ = {P0,P1}: $"firstCost"[11] + G[00] = "INF"$ ❌
- $Q_1$ = {P0}: $"firstCost"[01] + G[10] = 4 + 4 = 8$
- $Q_1$ = {P1}: $"firstCost"[10] + G[01] = 4 + 4 = 8$
- $"singleCost"[11] = bold(8)$, bestQ1 = {P0}

**完整 singleCost 表**:

| mask | singleCost[mask] | bestQ1 (首程子集) |
|---|---|---|
| 00 | 0 | — |
| 01 | 4 | {P0} |
| 10 | 4 | {P1} |
| 11 | 8 | {P0} |

**单车最终答案**: `singleCost[11] = 8`。

**回溯路径**: bestQ1[11] = {P0} → 首程是 $S -> P_0 -> T$ (距离 4), 剩余 mask = 11 ^ 01 = 10 = {P1}, pickG[10] = {P1} → 后续 trip $T -> P_1 -> T$ (距离 4)。完整路径: $S -> P_0 -> T -> P_1 -> T$, 总距离 = 8。✓

---

## 7 双车 DP + 对称消除 (加分项 1 + 创新点 ⭐)

**归约**: 双车把任务分为 $A$ 和 $[n] without A$, 每车独立按单车求解:
$"OPT"_("双车") = min_(A subset.eq [n]) "singleCost"[A] + "singleCost"[[n] without A]$

**朴素枚举**: 2^n 个 A, 每个 A 加法 O(1), 总 $O(2^n)$。但 (A, B) 与 (B, A) 是同一个无序划分, 被算了两次。

**对称消除**: 强制 $0 in A$ (i.e., 强制最低位的收集点属于第一辆车), 把 $2^n$ 个有序对压到 $2^(n-1) + 1$ 个无序划分 (额外的 +1 是 $A = emptyset$ 的退化情形, 单独处理)。

**枚举过程** (本例 n=2):

| 候选 | A (mask1) | B (mask2) | singleCost[A] | singleCost[B] | 合计 |
|---|---|---|---|---|---|
| 退化 | 0 = ∅ | 11 = {P0,P1} | 0 | 8 | 8 |
| 主 1 | 01 = {P0} | 10 = {P1} | 4 | 4 | **8** |
| 主 2 | 11 = {P0,P1} | 0 = ∅ | 8 | 0 | 8 |

强制 0 ∈ A 后, 主枚举只剩 mask1 ∈ {01, 11} (含 bit 0 的), 跳过了 {10}, {00} (除退化情形外)。

**双车最优答案**: 8 (与单车相同, 因为本例每个 trip 只能拿一个点, 双车并行也不能压缩总距离 — 该指标是"总距离"而非"最大时间")。

**实际工程意义**: 在本例中双车不比单车好, 但报告 §6.4 在三组规模递增样例上, 双车平均节省 $4.6%$–$11.8%$ 总距离。**关键加速来自不重建子距离矩阵**: 朴素实现对每个 A 都跑一次 `build_distance_matrix` + 子 DP, 优化后只跑一次主 DP + 查 singleCost 表, 实测 460× 加速。

---

## 8 最近邻贪心走一遍

**策略**: 每步从当前位置选**满足载重约束 + BFS 可达 + 未访问**的最近点; 无可装时回 T 卸货新 trip; 最后强制回 T。

**本例贪心过程**:

| 步骤 | 当前位置 | 当前载重 | 可装点 | 距离 | 选择 |
|---|---|---|---|---|---|
| 0 | S=(0,0) | 0 | P0, P1 | 都是 2 | P0 (tie-break: 先看到的) |
| 1 | P0=(0,2) | 2 | P1 (load 2+2=4>3, 拒) | — | 无可装 → 回 T |
| 2 | T=(2,2) | 0 (卸货) | P1 | 2 | P1 |
| 3 | P1=(2,0) | 2 | 无未访问 | — | 回 T |
| 4 | T=(2,2) | 0 | 全部完成 | — | 结束 |

**完整路径**: $S -> P_0 -> T -> P_1 -> T$, 总距离 = 4 + 4 = 8。

**本例下贪心 = DP** (因为问题对称), 但在不对称结构下贪心会陷阱化。报告 §6.5 的对抗实例 ($3 times 10$ 网格): DP=13, 贪心=15, 差 15.4%; 在 372 例随机样本上, 31% 严格次优, 最坏 +120%。

---

## 9 暴力枚举验证

**复杂度**: $n! cdot 2^(n-1)$ — 全排列 × 全部切分点。n=2 时 $2! cdot 2^1 = 4$ 种情况。

**穷举所有 4 种方案**:

| # | 访问顺序 | 切分点 | 行程 | 载重检查 | 总代价 |
|---|---|---|---|---|---|
| 1 | $P_0, P_1$ | 切在中间 | $S→P_0→T$ ; $T→P_1→T$ | 2≤3, 2≤3 ✓ | 4+4 = **8** |
| 2 | $P_0, P_1$ | 不切 | $S→P_0→P_1→T$ | 2+2=4>3 ❌ | INF |
| 3 | $P_1, P_0$ | 切在中间 | $S→P_1→T$ ; $T→P_0→T$ | 2≤3, 2≤3 ✓ | 4+4 = **8** |
| 4 | $P_1, P_0$ | 不切 | $S→P_1→P_0→T$ | 2+2=4>3 ❌ | INF |

**暴力最优 = 8**, 与 DP 答案完全一致 ✓ 。

这就是 `brute_force_checker.py` 在 720 例上做的事 — 用**独立的第二份 BFS** 算距离矩阵, 再 `itertools.permutations` 全排列 + 二进制位枚举切分点, 完全不复用 solver 的 DP 代码。结果 0 mismatch, 是 DP 实现正确的**直接证据**。

---

## 10 答案在表中的位置 (速查)

回到原题: "整个问题的最优解在哪里?"

| 问题 | 答案在哪 | 本例数值 |
|---|---|---|
| 单车最优总距离 | `singleCost[full]` (full = $2^n - 1$) | $"singleCost"[11] = 8$ |
| 单车首程子集 | `bestQ1[full]` | {P0} |
| 单车后续行程切分 | 从 `pickG[full ^ bestQ1[full]]` 递归 | {P1} |
| 双车最优总距离 | $min_(A: 0 in A or A = 0) "singleCost"[A] + "singleCost"[full ^ A]$ | 8 (任一种划分) |
| 双车两车的 mask | 枚举时记录的 bestM1, bestM2 | (取 (01, 10) 等) |
| 完整网格路径 | `expand_trip_path(dm, keySeq)` 沿关键点序列查 D 的 path 表 | $S → P_0 → T → P_1 → T$ |

整个 DP 的妙处: **所有答案都从同一组中间表 (tsp, firstCost, laterCost, G, singleCost) 派生**, 双车求解和单车求解共享 99% 的计算。这就是 P6 优化 460× 加速的本质。

---

## 11 看完这份文档你应该会回答的问题

✅ 距离矩阵是怎么构建的, 复杂度是多少
✅ tsp[mask][last] 的状态含义和转移
✅ firstCost 和 laterCost 的区别 (起点不同)
✅ G[mask] 的状态含义和填表顺序
✅ pivot 枚举对标准枚举的"等价但更快"是怎么回事
✅ singleCost[mask] 为什么是双车 DP 的基石
✅ 对称消除为什么强制 0∈A
✅ 暴力枚举怎么穷尽所有可能
✅ 哪个表的哪个 cell 装着最终答案
